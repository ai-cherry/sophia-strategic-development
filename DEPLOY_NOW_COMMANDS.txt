🚀 SOPHIA AI IMMEDIATE DEPLOYMENT COMMANDS
==========================================

⚡ QUICK DEPLOY (Copy and paste these commands):

1. Make the deployment script executable:
   chmod +x deploy_sophia_now.sh

2. Set your Docker Hub credentials (if not already logged in):
   docker login -u scoobyjava15

3. Run the complete deployment:
   ./deploy_sophia_now.sh

⚡ ALTERNATIVE: Manual deployment to primary server only:

# SSH into primary Lambda Labs server
ssh -i ~/.ssh/lambda_labs_key ubuntu@192.222.58.232

# Once connected, run these commands:
docker pull scoobyjava15/sophia-backend:latest
docker stop sophia-backend 2>/dev/null || true
docker rm sophia-backend 2>/dev/null || true
docker run -d \
  --name sophia-backend \
  --restart unless-stopped \
  -e ENVIRONMENT=prod \
  -p 8003:8000 \
  --network host \
  scoobyjava15/sophia-backend:latest

# Check deployment status
docker ps
curl http://localhost:8003/health

⚡ SUPER QUICK: Deploy just the backend API:

ssh -i ~/.ssh/lambda_labs_key ubuntu@192.222.58.232 'docker pull scoobyjava15/sophia-backend:latest && docker stop sophia-backend 2>/dev/null || true && docker rm sophia-backend 2>/dev/null || true && docker run -d --name sophia-backend --restart unless-stopped -e ENVIRONMENT=prod -p 8003:8000 --network host scoobyjava15/sophia-backend:latest && docker ps | grep sophia'

📋 PRE-DEPLOYMENT CHECKLIST:
✓ Lambda Labs SSH key at ~/.ssh/lambda_labs_key
✓ Docker Hub credentials (login with: docker login)
✓ GitHub repository access
✓ All Lambda Labs servers accessible

🔧 TROUBLESHOOTING:
- SSH key issues: chmod 600 ~/.ssh/lambda_labs_key
- Docker login: docker login -u scoobyjava15
- Permission denied: Run with sudo or check Docker group membership
- Connection timeout: Check Lambda Labs server status

🚀 GO GO GO!
