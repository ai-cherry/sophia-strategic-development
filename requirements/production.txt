# Production Dependencies
# Optimized dependencies for production deployment

-r base.txt

# Production-specific web server optimizations
gunicorn>=21.0.0
gevent>=23.0.0

# Enhanced monitoring and observability
sentry-sdk[fastapi]>=1.32.0
datadog>=0.47.0

# Performance optimizations (not in base.txt)
ujson>=5.8.0  # Faster JSON parsing than orjson
cython>=3.0.0  # Performance optimizations

# Security enhancements for production
cryptography>=41.0.0
bcrypt>=4.0.0

# Production-specific database optimizations (replaces base asyncpg)
# Note: This overrides the base asyncpg with speedups
asyncpg[speedups]==0.30.0

