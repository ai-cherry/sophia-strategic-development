apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mcp-notion
    service-tier: secondary
    tier: mcp
    version: v1.0.0
  name: mcp-notion
  namespace: sophia-mcp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mcp-notion
  template:
    metadata:
      labels:
        app: mcp-notion
        service-tier: secondary
        tier: mcp
    spec:
      containers:
      - env:
        - name: ENVIRONMENT
          value: prod
        - name: PULUMI_ORG
          value: scoobyjava-org
        - name: K3S_DEPLOYMENT
          value: 'true'
        - name: ENVIRONMENT
          value: ${ENVIRONMENT:-prod}
        - name: PULUMI_ORG
          value: ${PULUMI_ORG:-scoobyjava-org}
        - name: PORT
          value: '9003'
        - name: REDIS_URL
          value: redis://redis:6379
        envFrom:
        - secretRef:
            name: mcp-notion-secrets
        image: ${DOCKER_REGISTRY}/sophia-notion-v2:${IMAGE_TAG}
        livenessProbe:
          httpGet:
            path: /health
            port: mcp
          initialDelaySeconds: 30
          periodSeconds: 10
        name: notion
        ports:
        - containerPort: 9007
          name: mcp
        readinessProbe:
          httpGet:
            path: /ready
            port: mcp
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          limits:
            cpu: '1'
            memory: 2Gi
          requests:
            cpu: 500m
            memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-notion
  name: mcp-notion
  namespace: sophia-mcp
spec:
  ports:
  - name: mcp
    port: 9007
    protocol: TCP
    targetPort: mcp
  selector:
    app: mcp-notion
  type: ClusterIP
---
apiVersion: v1
data:
  server-config.json: "{\n  \"name\": \"notion\",\n  \"version\": \"1.0.0\",\n  \"\
    port\": 9007,\n  \"tier\": \"SECONDARY\",\n  \"description\": \"MCP notion server\"\
    \n}"
kind: ConfigMap
metadata:
  name: mcp-notion-config
  namespace: sophia-mcp
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: mcp-notion-hpa
  namespace: sophia-mcp
spec:
  maxReplicas: 5
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  - resource:
      name: memory
      target:
        averageUtilization: 80
        type: Utilization
    type: Resource
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: mcp-notion
