/**
 * Streamlined Ultra-Enhanced CEO Dashboard Component
 * Simplified but powerful executive dashboard with glassmorphism design
 * Generated by Sophia AI UI/UX Agent System - Phase 2 (Streamlined)
 */

import React, { useState, useEffect } from 'react';
import { 
  DollarSign, 
  Users, 
  TrendingUp, 
  Target, 
  AlertTriangle, 
  Calendar,
  Search,
  RefreshCw,
  Filter,
  BarChart3,
  PieChart,
  Activity,
  Brain,
  Zap,
  Shield,
  Globe
} from 'lucide-react';

// Enhanced mock data with AI insights
const enhancedKPIData = [
  {
    title: 'Total Revenue',
    value: '$2.4M',
    trend: 'up',
    trendValue: '+12.5%',
    description: 'AI predicts 18% growth next quarter',
    status: 'success',
    icon: DollarSign,
    aiInsight: 'Strong momentum in enterprise segment',
    confidence: 94
  },
  {
    title: 'Active Deals',
    value: '156',
    trend: 'up',
    trendValue: '+8.2%',
    description: 'Pipeline velocity increasing',
    status: 'success',
    icon: Target,
    aiInsight: 'Q4 conversion rate 23% above target',
    confidence: 87
  },
  {
    title: 'Team Efficiency',
    value: '94%',
    trend: 'up',
    trendValue: '+2.1%',
    description: 'Peak performance achieved',
    status: 'success',
    icon: Users,
    aiInsight: 'Optimal resource allocation detected',
    confidence: 91
  },
  {
    title: 'Market Share',
    value: '23.7%',
    trend: 'up',
    trendValue: '+1.8%',
    description: 'Competitive advantage growing',
    status: 'success',
    icon: Globe,
    aiInsight: 'Expansion opportunity in APAC',
    confidence: 89
  },
  {
    title: 'Customer Satisfaction',
    value: '4.8/5',
    trend: 'up',
    trendValue: '+0.3',
    description: 'Exceptional service delivery',
    status: 'success',
    icon: Shield,
    aiInsight: 'NPS score trending toward industry leadership',
    confidence: 92
  },
  {
    title: 'Innovation Index',
    value: '8.7/10',
    trend: 'up',
    trendValue: '+0.5',
    description: 'R&D investments paying off',
    status: 'success',
    icon: Brain,
    aiInsight: 'Patent portfolio strength increasing',
    confidence: 88
  }
];

const strategicAlerts = [
  {
    type: 'opportunity',
    title: 'Market Expansion Ready',
    message: 'AI analysis suggests optimal timing for European market entry',
    priority: 'high',
    confidence: 93,
    action: 'Review expansion strategy'
  },
  {
    type: 'risk',
    title: 'Competitor Activity',
    message: 'Increased competitive pressure in core segment detected',
    priority: 'medium',
    confidence: 86,
    action: 'Strengthen differentiation'
  },
  {
    type: 'insight',
    title: 'Customer Behavior Shift',
    message: 'Mobile engagement up 34% - mobile-first strategy recommended',
    priority: 'medium',
    confidence: 91,
    action: 'Accelerate mobile roadmap'
  }
];

const StreamlinedUltraEnhancedCEODashboard = () => {
  const [timeRange, setTimeRange] = useState('30d');
  const [refreshing, setRefreshing] = useState(false);
  const [searchQuery, setSearchQuery] = useState('');
  const [selectedMetrics, setSelectedMetrics] = useState('all');
  const [aiInsightsEnabled, setAiInsightsEnabled] = useState(true);

  // Enhanced refresh function
  const handleRefresh = async () => {
    setRefreshing(true);
    await new Promise(resolve => setTimeout(resolve, 2000));
    setRefreshing(false);
  };

  // Auto-refresh every 5 minutes
  useEffect(() => {
    const interval = setInterval(handleRefresh, 5 * 60 * 1000);
    return () => clearInterval(interval);
  }, []);

  // Filter KPI data based on search and selection
  const filteredKPIData = enhancedKPIData.filter(kpi => {
    const matchesSearch = !searchQuery || 
      kpi.title.toLowerCase().includes(searchQuery.toLowerCase()) ||
      kpi.description.toLowerCase().includes(searchQuery.toLowerCase());
    
    const matchesFilter = selectedMetrics === 'all' || 
      kpi.title.toLowerCase().includes(selectedMetrics.toLowerCase());
    
    return matchesSearch && matchesFilter;
  });

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 p-6">
      {/* Animated background elements */}
      <div className="fixed inset-0 overflow-hidden pointer-events-none">
        <div className="absolute -top-40 -right-40 w-80 h-80 bg-purple-500 rounded-full mix-blend-multiply filter blur-xl opacity-20 animate-pulse"></div>
        <div className="absolute -bottom-40 -left-40 w-80 h-80 bg-blue-500 rounded-full mix-blend-multiply filter blur-xl opacity-20 animate-pulse" style={{animationDelay: '2s'}}></div>
        <div className="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-80 h-80 bg-indigo-500 rounded-full mix-blend-multiply filter blur-xl opacity-10 animate-pulse" style={{animationDelay: '4s'}}></div>
      </div>

      <div className="relative z-10 max-w-7xl mx-auto">
        {/* Enhanced Header */}
        <div className="bg-gradient-to-r from-white/10 to-white/5 backdrop-blur-lg border border-white/20 rounded-2xl p-6 mb-8">
          <div className="flex items-center justify-between mb-6">
            <div>
              <h1 className="text-4xl font-bold text-white mb-2 flex items-center gap-3">
                <Brain className="w-10 h-10 text-blue-400" />
                Executive Command Center
                <span className="text-sm bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent font-normal">
                  AI-Powered
                </span>
              </h1>
              <p className="text-gray-300 text-lg">
                Real-time insights • Predictive analytics • Strategic intelligence
              </p>
            </div>
            <div className="flex items-center gap-4">
              <div className="flex items-center gap-2 text-green-400">
                <div className="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
                <span className="text-sm font-medium">Live Data</span>
              </div>
              <button
                onClick={handleRefresh}
                disabled={refreshing}
                className="flex items-center gap-2 px-4 py-2 bg-white/10 hover:bg-white/20 border border-white/20 rounded-lg transition-all duration-300 text-white"
              >
                <RefreshCw className={`w-4 h-4 ${refreshing ? 'animate-spin' : ''}`} />
                {refreshing ? 'Updating...' : 'Refresh'}
              </button>
            </div>
          </div>

          {/* AI Insights Toggle */}
          {aiInsightsEnabled && (
            <div className="bg-gradient-to-r from-blue-500/20 to-purple-500/20 border border-blue-400/30 rounded-lg p-4">
              <div className="flex items-center gap-3">
                <Zap className="w-5 h-5 text-yellow-400" />
                <span className="text-white font-medium">AI Insights Active</span>
                <span className="text-gray-300 text-sm">
                  Analyzing 847 data points • Confidence: 91% • Last updated: 2 min ago
                </span>
              </div>
            </div>
          )}
        </div>

        {/* Enhanced Control Panel */}
        <div className="bg-white/5 backdrop-blur-lg border border-white/10 rounded-xl p-4 mb-8">
          <div className="flex flex-wrap items-center justify-between gap-4">
            <div className="flex items-center gap-4">
              {/* Time Range Selector */}
              <div className="flex items-center gap-2">
                <Calendar className="w-4 h-4 text-gray-400" />
                <select
                  value={timeRange}
                  onChange={(e) => setTimeRange(e.target.value)}
                  className="bg-white/10 border border-white/20 rounded-lg px-3 py-2 text-white text-sm focus:outline-none focus:ring-2 focus:ring-blue-400"
                >
                  <option value="24h">Last 24 Hours</option>
                  <option value="7d">Last 7 Days</option>
                  <option value="30d">Last 30 Days</option>
                  <option value="90d">Last 90 Days</option>
                  <option value="1y">Last Year</option>
                </select>
              </div>
            </div>

            <div className="flex items-center gap-4">
              {/* Search */}
              <div className="relative">
                <Search className="absolute left-3 top-1/2 transform -translate-y-1/2 w-4 h-4 text-gray-400" />
                <input
                  type="text"
                  placeholder="Search metrics..."
                  value={searchQuery}
                  onChange={(e) => setSearchQuery(e.target.value)}
                  className="bg-white/10 border border-white/20 rounded-lg pl-10 pr-4 py-2 text-white placeholder-gray-400 text-sm focus:outline-none focus:ring-2 focus:ring-blue-400 w-64"
                />
              </div>

              {/* Metrics Filter */}
              <div className="flex items-center gap-2">
                <Filter className="w-4 h-4 text-gray-400" />
                <select
                  value={selectedMetrics}
                  onChange={(e) => setSelectedMetrics(e.target.value)}
                  className="bg-white/10 border border-white/20 rounded-lg px-3 py-2 text-white text-sm focus:outline-none focus:ring-2 focus:ring-blue-400"
                >
                  <option value="all">All Metrics</option>
                  <option value="revenue">Revenue</option>
                  <option value="efficiency">Efficiency</option>
                  <option value="market">Market</option>
                </select>
              </div>
            </div>
          </div>
        </div>

        {/* Strategic Alerts */}
        <div className="mb-8">
          <h2 className="text-2xl font-bold text-white mb-4 flex items-center gap-2">
            <AlertTriangle className="w-6 h-6 text-yellow-400" />
            Strategic Intelligence
          </h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            {strategicAlerts.map((alert, index) => (
              <div
                key={index}
                className="bg-white/5 backdrop-blur-lg border border-white/10 rounded-lg p-4 hover:bg-white/10 transition-all duration-300"
              >
                <div className="flex items-start justify-between mb-2">
                  <span className={`px-2 py-1 rounded-full text-xs font-medium ${
                    alert.type === 'opportunity' ? 'bg-green-500/20 text-green-400' :
                    alert.type === 'risk' ? 'bg-red-500/20 text-red-400' :
                    'bg-blue-500/20 text-blue-400'
                  }`}>
                    {alert.type.toUpperCase()}
                  </span>
                  <span className="text-xs text-gray-400">{alert.confidence}% confidence</span>
                </div>
                <h3 className="text-white font-semibold mb-1">{alert.title}</h3>
                <p className="text-gray-300 text-sm mb-3">{alert.message}</p>
                <button className="text-blue-400 hover:text-blue-300 text-sm font-medium transition-colors">
                  {alert.action} →
                </button>
              </div>
            ))}
          </div>
        </div>

        {/* Enhanced KPI Grid */}
        <div className="mb-8">
          <h2 className="text-2xl font-bold text-white mb-4 flex items-center gap-2">
            <BarChart3 className="w-6 h-6 text-blue-400" />
            Key Performance Indicators
          </h2>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {filteredKPIData.map((kpi, index) => (
              <div
                key={index}
                className="group relative bg-white/5 backdrop-blur-lg border border-white/10 rounded-xl p-6 hover:bg-white/10 hover:border-white/20 transition-all duration-300 hover:scale-105"
              >
                {/* AI Insight Badge */}
                {aiInsightsEnabled && kpi.aiInsight && (
                  <div className="absolute top-4 right-4 bg-gradient-to-r from-purple-500/20 to-blue-500/20 border border-purple-400/30 rounded-full px-2 py-1">
                    <span className="text-xs text-purple-300 font-medium">AI</span>
                  </div>
                )}

                <div className="flex items-start justify-between mb-4">
                  <div className="flex items-center gap-3">
                    <div className="p-2 bg-white/10 rounded-lg">
                      <kpi.icon className="w-6 h-6 text-blue-400" />
                    </div>
                    <div>
                      <h3 className="text-white font-semibold">{kpi.title}</h3>
                      <p className="text-gray-400 text-sm">{kpi.description}</p>
                    </div>
                  </div>
                </div>

                <div className="mb-4">
                  <div className="flex items-baseline gap-2 mb-2">
                    <span className="text-3xl font-bold text-white">{kpi.value}</span>
                    <span className={`flex items-center gap-1 text-sm font-medium ${
                      kpi.trend === 'up' ? 'text-green-400' : 
                      kpi.trend === 'down' ? 'text-red-400' : 'text-gray-400'
                    }`}>
                      <TrendingUp className="w-4 h-4" />
                      {kpi.trendValue}
                    </span>
                  </div>
                  
                  {/* Confidence Indicator */}
                  {aiInsightsEnabled && kpi.confidence && (
                    <div className="flex items-center gap-2 mb-2">
                      <div className="flex-1 bg-white/10 rounded-full h-1">
                        <div 
                          className="bg-gradient-to-r from-blue-400 to-purple-400 h-1 rounded-full transition-all duration-1000"
                          style={{ width: `${kpi.confidence}%` }}
                        ></div>
                      </div>
                      <span className="text-xs text-gray-400">{kpi.confidence}%</span>
                    </div>
                  )}
                </div>

                {/* AI Insight */}
                {aiInsightsEnabled && kpi.aiInsight && (
                  <div className="bg-gradient-to-r from-purple-500/10 to-blue-500/10 border border-purple-400/20 rounded-lg p-3 mt-4">
                    <div className="flex items-center gap-2 mb-1">
                      <Brain className="w-4 h-4 text-purple-400" />
                      <span className="text-xs text-purple-300 font-medium">AI Insight</span>
                    </div>
                    <p className="text-sm text-gray-300">{kpi.aiInsight}</p>
                  </div>
                )}
              </div>
            ))}
          </div>
        </div>

        {/* Simplified Charts Section */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-8">
          <div className="bg-white/5 backdrop-blur-lg border border-white/10 rounded-xl p-6">
            <h3 className="text-xl font-bold text-white mb-4 flex items-center gap-2">
              <PieChart className="w-5 h-5 text-green-400" />
              Revenue Analytics
            </h3>
            <div className="h-64 flex items-center justify-center">
              <div className="text-center">
                <div className="text-4xl font-bold text-green-400 mb-2">$2.4M</div>
                <div className="text-gray-300 mb-2">Monthly Revenue</div>
                <div className="flex items-center justify-center gap-2 text-green-400">
                  <TrendingUp className="w-4 h-4" />
                  <span className="text-sm">+12.5% vs last month</span>
                </div>
              </div>
            </div>
          </div>

          <div className="bg-white/5 backdrop-blur-lg border border-white/10 rounded-xl p-6">
            <h3 className="text-xl font-bold text-white mb-4 flex items-center gap-2">
              <Activity className="w-5 h-5 text-purple-400" />
              Performance Metrics
            </h3>
            <div className="h-64 flex items-center justify-center">
              <div className="text-center">
                <div className="text-4xl font-bold text-purple-400 mb-2">94%</div>
                <div className="text-gray-300 mb-2">Team Efficiency</div>
                <div className="flex items-center justify-center gap-2 text-purple-400">
                  <TrendingUp className="w-4 h-4" />
                  <span className="text-sm">+2.1% improvement</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Footer */}
        <div className="text-center">
          <p className="text-gray-400 text-sm">
            Powered by Sophia AI • Last updated: {new Date().toLocaleTimeString()} • 
            <span className="text-blue-400 ml-1">Streamlined Ultra-Enhanced Dashboard v2.1</span>
          </p>
        </div>
      </div>
    </div>
  );
};

export default StreamlinedUltraEnhancedCEODashboard;

