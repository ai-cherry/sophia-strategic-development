apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This base is intended to be used by environment-specific overlays
# Each MCP server directory should have its own deployment.yaml

resources:
  # Common resources can be added here
  # - namespace.yaml
  # - network-policy.yaml
