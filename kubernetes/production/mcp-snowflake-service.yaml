apiVersion: v1
kind: Service
metadata:
  name: mcp-snowflake
  namespace: sophia-ai-prod
  labels:
    app: mcp-snowflake
    component: mcp-server
    server: snowflake
spec:
  selector:
    app: mcp-snowflake
  ports:
  - port: 9002
    targetPort: 9002
    name: http
  type: ClusterIP
