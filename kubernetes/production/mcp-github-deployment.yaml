apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-github
  namespace: sophia-ai-prod
  labels:
    app: mcp-github
    component: mcp-server
    server: github
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mcp-github
  template:
    metadata:
      labels:
        app: mcp-github
        component: mcp-server
        server: github
    spec:
      nodeSelector:
        kubernetes.io/hostname: sophia-mcp-orchestrator
      containers:
      - name: mcp-github
        image: scoobyjava15/sophia-mcp-github:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 9005
          name: http
        env:
        - name: ENVIRONMENT
          value: "prod"
        - name: PULUMI_ORG
          value: "scoobyjava-org"
        - name: MCP_SERVER_NAME
          value: "github"
        - name: MCP_SERVER_PORT
          value: "9005"
        livenessProbe:
          httpGet:
            path: /health
            port: 9005
          initialDelaySeconds: 30
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 9005
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m" 