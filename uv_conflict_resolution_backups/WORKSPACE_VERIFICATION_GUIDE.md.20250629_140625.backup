# 🎯 Sophia AI Workspace Verification Guide

## ✅ Current Status: VERIFIED AND READY

**You are correctly positioned in the Sophia AI development workspace!**

---

## 📍 Correct Location Checklist

### **Required Directory:**
```
/Users/lynnmusil/sophia-main/
```

### **Key Indicators You're in the Right Place:**
- ✅ Terminal shows: `lynnmusil@Lynns-MacBook-Pro ~/sophia-main %`
- ✅ `pwd` returns: `/Users/lynnmusil/sophia-main`
- ✅ Virtual environment active: `/Users/lynnmusil/sophia-main/.venv/bin/python`
- ✅ Environment variables set: `ENVIRONMENT=prod`, `PULUMI_ORG=scoobyjava-org`

---

## 🛠️ Quick Verification Commands

### **Instant Workspace Check:**
```bash
verify-sophia          # Comprehensive workspace verification
```

### **Navigation & Setup:**
```bash
go-sophia             # Navigate to project + activate environment
ready-to-code         # Full setup verification
sophia-status         # Quick status check
```

### **Manual Verification:**
```bash
pwd                   # Should show: /Users/lynnmusil/sophia-main
which python          # Should show: /Users/lynnmusil/sophia-main/.venv/bin/python
echo $VIRTUAL_ENV     # Should show: /Users/lynnmusil/sophia-main/.venv
```

---

## 🚨 Warning Signs You're in the Wrong Place

### **Red Flags:**
- ❌ Terminal shows different directory path
- ❌ `which python` points to system Python (not .venv)
- ❌ Missing files: `pyproject.toml`, `uv.lock`, `.cursorrules`
- ❌ `VIRTUAL_ENV` not set or pointing elsewhere

### **Quick Fix:**
```bash
go-sophia             # This will fix most issues
```

---

## 🔧 For Cursor AI Assistant

### **Before Every Coding Session:**
1. **Run workspace verification:**
   ```bash
   verify-sophia
   ```

2. **Confirm key files exist:**
   - `backend/app/fastapi_app.py`
   - `frontend/package.json`
   - `pyproject.toml`
   - `uv.lock`

3. **Check environment:**
   - Python: `/Users/lynnmusil/sophia-main/.venv/bin/python`
   - Virtual Env: Active and correct
   - Environment: `prod`
   - Pulumi Org: `scoobyjava-org`

### **Safe Development Practices:**
- ✅ Always verify workspace before making changes
- ✅ Use UV commands: `uv sync`, `uv add`, `uv run`
- ✅ Check git status: `git status`, `git branch`
- ✅ Test imports: `python -c "from backend.core.auto_esc_config import get_config_value"`

---

## 📂 Project Structure Reference

```
/Users/lynnmusil/sophia-main/
├── backend/                 # Python backend code
├── frontend/               # React frontend code
├── mcp-servers/           # MCP server implementations
├── infrastructure/        # Pulumi infrastructure code
├── scripts/               # Utility scripts
├── pyproject.toml         # UV dependencies
├── uv.lock               # Locked dependencies
├── .cursorrules          # Cursor AI rules
└── .venv/                # Virtual environment
```

---

## 🎉 Success Indicators

When everything is correct, you should see:
```
🎉 WORKSPACE VERIFICATION: ✅ PERFECT!
🚀 Ready for Sophia AI development!
```

---

## 🆘 Emergency Recovery

If you ever get lost or things break:

1. **Navigate back:**
   ```bash
   cd ~/sophia-main
   ```

2. **Reactivate environment:**
   ```bash
   source .venv/bin/activate
   ```

3. **Full verification:**
   ```bash
   verify-sophia
   ```

4. **If still broken, contact support with output of:**
   ```bash
   pwd && which python && echo $VIRTUAL_ENV && ls -la
   ```

---

**Remember: When in doubt, run `verify-sophia` first!** 🎯
