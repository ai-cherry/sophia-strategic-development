apiVersion: v1
kind: Service
metadata:
  name: sophia-backend
  labels:
    app: sophia-backend
spec:
  selector:
    app: sophia-backend
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: LoadBalancer 